<view class="container">
  <search autoFocus="{{ false }}" bind:tap="onInputFocus"></search>
  <!-- <view class="account-list">
      <view class="account-card" 
        wx:for="{{ accountList }}" 
        wx:for-index="idex" 
        wx:for-item="item"
        wx:key="idx"
        data-info="{{ item }}"
        bind:tap="toDetail">
        <view class="account-icon" 
          style="background-color: {{ item.color || tools.randomColor() }}">{{ tools.getSliceName(item.name) }}</view>
        <view class="account-info">
          <view class="account-name line-ellipsis">{{ item.name }}</view>
          <view class="account-description line-ellipsis">{{ item.description }}</view>
        </view>
      </view>
  </view> -->
  <accountList accountList="{{ accountList }}"></accountList>

  <view class="add-button animation-pulse" bind:tap="scanCode">
    <image src="../../assets/add.png" class="add-icon"/>
  </view>
</view>
